fork()为返回值函数 0-65535
父->子进程的PID(进程号) pid>0
fork复制子进程实际上是把父进程在执行fork前为数据情况和完整的代码复制给子进程

父子进程拥有相同的代码，但可通过fork返回值不同使得父子进程执行不同的代码段

printf()里面数据如果不加\n代表全缓冲，输入后不清空;加了\n代表行缓冲，输入后清空;
write输入后清空。如果使用输出转向>可以永久保存，无需管printf是否有\n。
